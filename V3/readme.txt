本版本继续完成请求的解析工作(解析消息头)
消息头是由若干行组成，每个消息头的格式为:消息头的名字: 消息头的值(CRLF)
因此我们需要循环读取每一行(直到单独读取到了CRLF)，然后按照冒号空格拆分消息头。

这里需要先将读取一行字符串的操作提成一个方法，这样才可以复用这个功能。
在ClientHandler中定义一个方法，名为readLine,用来读取一行字符串。然后将解析请求行时读取
一行字符串的操作改为调用这个方法，并在后续解析消息头也使用这个方法。